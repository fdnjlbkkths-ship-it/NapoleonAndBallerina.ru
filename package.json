{
  "name": "sweet-master",
  "version": "1.0.0",
  "description": "Адаптивный сайт кондитерской Sweet Master",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:pages": "VITE_BASE=/NapoleonAndBallerina.ru/ vite build && mkdir -p assets && cp -f dist/assets/game.js dist/assets/game.css assets/ && cp -f public/favicon.svg favicon.svg && ! grep -q 'import\".*modulepreload' assets/game.js",
    "preview": "vite preview",
    "test:game": "node src/game/engine.test.mjs && node src/game/promo.test.mjs"
  },
  "devDependencies": {
    "sass": "^1.77.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "gsap": "^3.12.5"
  }
}
